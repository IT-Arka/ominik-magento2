<?xml version="1.0" ?>
<config xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="urn:magento:framework-message-queue:etc/publisher.xsd">
    <!-- PUBLISHER PRICE UPDATE -->
    <publisher topic="price.update">
        <connection name="amqp" exchange="price.update"/>
    </publisher>

    <!-- PUBLISHER STOCK UPDATE -->
    <publisher topic="stock.update">
        <connection name="amqp" exchange="stock.update"/>
    </publisher>
</config>
